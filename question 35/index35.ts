// T-Shirt: Write a function called make_shirt() 
// that accepts a size and the text of a message 
// that should be printed on the shirt. The function 
// should print a sentence summarizing the size of the
//  shirt and the message printed on it. Call the function.

function make_shirt(size:string,message:string){
    console.log(` you order the ${size} size shirt with message like ${message}\n`)
}
make_shirt(`medium`,`i love pakistan`)
make_shirt(`large`,`love to eat food`)